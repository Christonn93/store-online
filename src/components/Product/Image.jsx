// Importing React
import React from "react";

// TODO
/**
 * 
 * @param {*} param0 
 * @returns 
 */
const Image = ({data}) => {
 return <img src={data} alt={data} loading="lazy" />;
};

export default Image;
